// Código a ser lido pelo node
console.log("nodejs"); // podemos executar isso no navegador também além do terminal

// usamos 'cd' no terminal para selecionar outra pasta

// código que vai ser executado na página
alert("exibindo na página!"); 
// no nodejs não conseguimos executar o alert, porque o nodejs nao possui o objeto window do navegador

// erro: alert is not defined 
