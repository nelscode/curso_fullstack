// função que recebe dois numeros e retorna o maior deles

const maiorNumero = (a, b) => a > b ? a : b  // função mais curta possível (não é necessário return e nem as chaves)
console.log(maiorNumero(40, 2))